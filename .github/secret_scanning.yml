# Vendored detection rules and the test corpus intentionally contain fake credential
# samples (true-positive test cases for the rules). They are not real secrets.
paths-ignore:
  - 'rules/atr/**'
  - 'rules/fixtures/**'
  - 'packages/core/src/rules.bundle.json'
