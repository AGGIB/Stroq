## Summary

<!-- What does this change do, and why? -->

## Checklist

- [ ] Tests added or updated (including a regression test for any bug fix)
- [ ] `pnpm build:rules` run and its output committed, if anything under `rules/` changed
- [ ] `pnpm test:coverage`, `pnpm typecheck`, and `pnpm format:check` all pass locally
- [ ] Docs updated (README, `packages/cli/README.md`, or `CONTRIBUTING.md`) if behavior, commands, or policy defaults changed

## Test plan

<!-- How did you verify this change? Commands run, fixtures added, manual steps. -->
